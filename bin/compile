#!/usr/bin/env bash

set -e

YELLOW='\033[1;33m'
RESET='\033[0m'

function indent() {
  c='s/^/       /'
  case $(uname) in
    Darwin) sed -l "$c";;
    *)      sed -u "$c";;
  esac
}

unset GIT_DIR
